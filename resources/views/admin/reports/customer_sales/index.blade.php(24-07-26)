@extends('admin.layouts.appnew')

@section('content')


<div class="page-body">
    <div class="container-fluid page-body-wrapper">
        <div class="main-panel">
            <div class="content-wrapper">

                <div class="row">
                    <div class="col-lg-12 grid-margin stretch-card">
                        <div class="card">
                            <div class="card-body">

                             {{-- Success Message --}}
                                @if (session('success'))
                                    <div class="alert alert-success">
                                        {{ session('success') }}
                                    </div>
                                @endif

                                <div class="d-flex justify-content-between align-items-center mb-3">
                                    <h3 class="mb-4">Customer Sales Report</h3>
                                </div>



<form method="GET" class="mb-3">

    <div style="display:flex; gap:10px; align-items:end;">

        <div>
            <label>From Date</label>
            <input type="date" name="from" value="{{ $from ?? '' }}" class="form-control">
        </div>

        <div>
            <label>To Date</label>
            <input type="date" name="to" value="{{ $to ?? '' }}" class="form-control">
        </div>

        <div>
            <button type="submit" class="btn btn-success">
                Filter
            </button>

            <a href="{{ route('customer.sales.report') }}" class="btn btn-secondary">
                Reset
            </a>
        </div>

    </div>

</form>

 <div class="table-responsive">
                                    <table class="table all-package theme-table" id="stockTable">
                                        <thead class="b-shadow">
        <tr>
            <th>ID</th>
            <th>Customer Name</th>
            <th>Outlet Name</th>
            <th>Total Orders</th>
            <th>Total Amount</th>
            <th>Action</th>
        </tr>
    </thead>

    <tbody>
        @foreach($data as $row)
        <tr>
            <td>{{ $row->id }}</td>
            <td>{{ $row->customer_name }}</td>
            <td>{{ $row->outlet_name ?? '-' }}</td>
            <td>{{ $row->order_count }}</td>
            <td>₹ {{ number_format($row->total_amount, 2) }}</td>
            <td>
               <a href="{{ route('customer.sales.report.pdf', [
        'id' => $row->id,
        'from' => request('from'),
        'to' => request('to')
    ]) }}"
   class="btn btn-sm btn-primary">
    Download PDF
</a>
            </td>
        </tr>
        @endforeach
    </tbody>
</table>
</div>
      <div class="mt-4 p-3 bg-light border rounded">

    <h5 class="mb-2">
        Overall Orders:
        <strong>{{ $overallTotalOrders }}</strong>
    </h5>

    <!-- Add total amount for sales order with gst and without gst -->
     <h5 class="mb-0">
        Overall Basic Sales Amount (Excluding GST):
        <strong>₹ {{ number_format($overallTotalAmount, 2)  }}</strong>
    </h5>
   </br>
   <h5 class="mb-0">
    Overall Sales Amount (Including GST):
    <strong>₹ {{ number_format($overallTotalWithGST, 2) }}</strong>
</h5>


   </div>  

                            </div> {{-- card-body --}}
                        </div>
                    </div>
                </div>

            </div>
        </div>
    </div>
</div>
@endsection
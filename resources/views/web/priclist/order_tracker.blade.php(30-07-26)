@extends('mobile.mobile-app')
@section('content')

<style>
.ot-page {
    background: #f7f8fa;
    padding: 16px 16px 24px;
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
}
.ot-container { max-width: 720px; margin: 0 auto; }

.ot-title { font-size: 24px; font-weight: 800; color: #101828; margin: 0 0 16px; }
.ot-title .accent { color: #2f5ede; }

/* ===== Tabs ===== */
.ot-tabs {
    display: flex;
    flex-wrap: wrap;
    gap: 18px;
    row-gap: 8px;
    border-bottom: 1px solid #eef0f3;
    margin-bottom: 16px;
}
.ot-tab {
    display: flex;
    align-items: center;
    gap: 6px;
    padding: 10px 2px 14px;
    font-size: 14px;
    font-weight: 700;
    color: #667085;
    cursor: pointer;
    border: none;
    background: none;
    border-bottom: 2px solid transparent;
    white-space: nowrap;
    flex-shrink: 0;
}
.ot-tab.active { color: #101828; border-bottom-color: #e0442e; }
.ot-tab.active .ot-tab-count { background: #ffe1d1; color: #d85a30; }
.ot-tab-count {
    background: #eef0f3;
    color: #667085;
    font-size: 11px;
    font-weight: 700;
    padding: 2px 8px;
    border-radius: 100px;
    flex-shrink: 0;
}

@media (max-width: 420px) {
    .ot-tabs { gap: 12px; }
    .ot-tab { font-size: 12.5px; padding-bottom: 12px; }
    .ot-tab-count { font-size: 10px; padding: 2px 6px; }
}

@media (max-width: 360px) {
    .ot-tabs { gap: 10px; }
    .ot-tab { font-size: 11.5px; }
}

/* ===== Status summary cards ===== */
.ot-status-grid {
    display: grid;
    grid-template-columns: repeat(5, 1fr);
    gap: 6px;
    margin-bottom: 16px;
}
.ot-status-card {
    background: #fff;
    border: 1px solid #eef0f3;
    border-radius: 12px;
    padding: 10px 4px;
    text-align: center;
    min-width: 0;
}
.ot-status-clickable { cursor: pointer; transition: border-color .15s, box-shadow .15s; }
.ot-status-clickable:hover { border-color: #d0d5dd; }
.ot-status-clickable.active { border-color: #4f5fff; box-shadow: 0 0 0 1px #4f5fff; }
.ot-status-icon { margin-bottom: 6px; }
.ot-status-icon svg { width: 18px; height: 18px; }
.ot-status-label {
    font-size: 9px;
    font-weight: 600;
    color: #344054;
    margin-bottom: 3px;
    line-height: 1.2;
    overflow-wrap: break-word;
}
.ot-status-count { font-size: 14px; font-weight: 800; color: #101828; }

@media (max-width: 400px) {
    .ot-status-grid { gap: 4px; }
    .ot-status-card { padding: 8px 2px; }
    .ot-status-icon svg { width: 16px; height: 16px; }
    .ot-status-label { font-size: 8px; }
    .ot-status-count { font-size: 12.5px; }
}

@media (min-width: 560px) {
    .ot-status-card { padding: 14px 8px; }
    .ot-status-icon svg { width: 22px; height: 22px; }
    .ot-status-label { font-size: 11px; }
    .ot-status-count { font-size: 17px; }
}

@media (min-width: 560px) {
    .ot-status-grid { grid-template-columns: repeat(5, 1fr); }
}

/* ===== Filters ===== */

/* ===== Filters ===== */
.ot-filters {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 8px;
    margin-bottom: 12px;
}
.ot-filter-field label {
    display: block;
    font-size: 11px;
    font-weight: 700;
    color: #344054;
    margin-bottom: 5px;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
}
.ot-filter-field select,
.ot-filter-field input {
    width: 100%;
    box-sizing: border-box;
    border: 1px solid #e4e7ec;
    border-radius: 9px;
    padding: 8px 8px;
    font-size: 11.5px;
    color: #344054;
    background: #fff;
    min-width: 0;
}

@media (max-width: 400px) {
    .ot-filters { gap: 6px; }
    .ot-filter-field label { font-size: 10px; }
    .ot-filter-field select,
    .ot-filter-field input { padding: 7px 6px; font-size: 10.5px; }
}

@media (min-width: 560px) {
    .ot-filter-field label { font-size: 12.5px; margin-bottom: 6px; }
    .ot-filter-field select,
    .ot-filter-field input { padding: 10px 12px; font-size: 13px; }
}

.ot-filter-actions { display: flex; gap: 10px; margin-bottom: 20px; }
.ot-btn-apply {
    background: #e2571f;
    color: #fff;
    border: none;
    border-radius: 9px;
    padding: 11px 20px;
    font-size: 13.5px;
    font-weight: 700;
    cursor: pointer;
}
.ot-btn-reset {
    background: #fff;
    color: #344054;
    border: 1px solid #e4e7ec;
    border-radius: 9px;
    padding: 11px 20px;
    font-size: 13.5px;
    font-weight: 700;
    cursor: pointer;
}

.ot-section-label { font-size: 15px; font-weight: 700; color: #101828; margin-bottom: 12px; }


/* ===== Order card ===== */
.ot-order-card {
    background: #fff;
    border: 1px solid #eef0f3;
    border-radius: 14px;
    padding: 16px;
    margin-bottom: 14px;
}
.ot-order-top {
    display: flex;
    gap: 12px;
    margin-bottom: 14px;
}
.ot-order-icon {
    width: 42px; height: 42px; border-radius: 10px;
    display: flex; align-items: center; justify-content: center;
    flex-shrink: 0;
}
.ot-order-mid { flex: 1; min-width: 0; }
.ot-order-id-row { display: flex; align-items: center; gap: 8px; flex-wrap: wrap; margin-bottom: 4px; }
.ot-order-id { font-size: 15px; font-weight: 800; color: #101828; }
.ot-status-badge {
    font-size: 11px;
    font-weight: 700;
    padding: 3px 10px;
    border-radius: 100px;
    white-space: nowrap;
}
.ot-order-outlet { font-size: 12.5px; color: #667085; margin-bottom: 6px; }
.ot-order-meta { display: flex; align-items: center; gap: 14px; flex-wrap: wrap; font-size: 12px; color: #98a2b3; }
.ot-order-meta a { color: #4f5fff; font-weight: 600; text-decoration: none; display: inline-flex; align-items: center; gap: 4px; }

.ot-order-right { text-align: right; flex-shrink: 0; }
.ot-order-right-label { font-size: 11px; color: #98a2b3; margin-bottom: 2px; }
.ot-order-right-date { font-size: 13px; font-weight: 700; margin-bottom: 10px; white-space: nowrap; }
.ot-order-amount-label { font-size: 11px; color: #98a2b3; margin-bottom: 2px; }
.ot-order-amount { font-size: 14.5px; font-weight: 800; color: #101828; margin-bottom: 10px; }
.ot-payment-badge {
    font-size: 10.5px;
    font-weight: 700;
    padding: 3px 9px;
    border-radius: 100px;
    display: inline-block;
    margin-bottom: 8px;
}
.ot-pay-now-btn {
    background: #2f3fae;
    color: #fff;
    border: none;
    border-radius: 8px;
    padding: 8px 16px;
    font-size: 12.5px;
    font-weight: 700;
    cursor: pointer;
    white-space: nowrap;
    display: block;
    width: 100%;
    text-decoration: none;
    text-align: center;
}

/* Mobile: stack the right-side details below the main content row
   instead of cramming everything into a narrow third column */
@media (max-width: 480px) {
    .ot-order-top { flex-wrap: wrap; }
    .ot-order-mid { flex-basis: 100%; }
    .ot-order-right {
        flex-basis: 100%;
        text-align: left;
        margin-top: 12px;
        padding-top: 12px;
        border-top: 1px solid #f2f3f6;
        display: flex;
        flex-wrap: wrap;
        align-items: center;
        gap: 10px 16px;
    }
    .ot-order-right-label,
    .ot-order-amount-label { display: none; }
    .ot-order-right-date::before { content: 'Expected: '; font-weight: 400; color: #98a2b3; }
    .ot-order-right-date.delivered-date::before { content: 'Delivered: '; }
    .ot-order-amount::before { content: 'Total: '; font-weight: 400; color: #98a2b3; font-size: 12px; }
    .ot-order-right-date,
    .ot-order-amount,
    .ot-payment-badge { margin-bottom: 0; }
    .ot-pay-now-btn { width: auto; margin-left: auto; }
}

/* ===== Stepper ===== */
.ot-stepper {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    position: relative;
    margin-top: 8px;
}
.ot-stepper::before {
    content: '';
    position: absolute;
    top: 13px;
    left: 13px;
    right: 13px;
    height: 2px;
    background: #e4e7ec;
    z-index: 0;
}
.ot-step {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 6px;
    position: relative;
    z-index: 1;
    flex: 1;
}
.ot-step-dot {
    width: 26px; height: 26px; border-radius: 50%;
    background: #fff;
    border: 2px solid #d0d5dd;
    display: flex; align-items: center; justify-content: center;
    color: #d0d5dd;
}
.ot-step.done .ot-step-dot { border-color: #1d9e75; background: #1d9e75; color: #fff; }
.ot-step.current .ot-step-dot { border-color: #e0442e; color: #e0442e; background: #fff; }
.ot-step-label { font-size: 10px; font-weight: 600; color: #98a2b3; text-align: center; }
.ot-step.done .ot-step-label,
.ot-step.current .ot-step-label { color: #344054; }

.ot-stepper-line-fill {
    position: absolute;
    top: 13px;
    left: 13px;
    height: 2px;
    background: #1d9e75;
    z-index: 0;
}

@media (min-width: 768px) {
    .ot-page { padding: 32px 24px; }
    .ot-container {
        background: #fff;
        border-radius: 20px;
        padding: 28px 32px;
        box-shadow: 0 1px 3px rgba(16,24,40,0.05), 0 1px 2px rgba(16,24,40,0.04);
    }
}
</style>

@php
    $statusSteps = ['in_review', 'in_progress', 'ready_dispatch', 'dispatched', 'delivered'];
    $statusLabels = [
        'in_review'      => 'In Review',
        'in_progress'    => 'In Progress',
        'ready_dispatch' => 'Ready to Dispatch',
        'dispatched'     => 'Dispatched',
        'delivered'      => 'Delivered',
    ];
    $statusColors = [
        'in_review'      => ['bg' => '#eef2ff', 'fg' => '#2f5ede'],
        'in_progress'    => ['bg' => '#fdecea', 'fg' => '#e0442e'],
        'ready_dispatch' => ['bg' => '#fff3e0', 'fg' => '#d97706'],
        'dispatched'     => ['bg' => '#eef2ff', 'fg' => '#2f5ede'],
        'delivered'      => ['bg' => '#e8f8f0', 'fg' => '#1d9e75'],
    ];

    // $liveOrders, $historyOrders, $cancelledOrders, $statusCounts, $outlets,
    // $month, $orderId, $outlet all come from the controller now.
@endphp

<div class="ot-page">
    <div class="ot-container">

        <div class="ot-title">Your <span class="accent">Orders</span></div>

        <!-- ===== Tabs ===== -->
        <div class="ot-tabs">
            <button type="button" class="ot-tab active" data-tab="live">
                Live Orders <span class="ot-tab-count">{{ count($liveOrders) }}</span>
            </button>
            <button type="button" class="ot-tab" data-tab="history">
                Order History <span class="ot-tab-count">{{ count($historyOrders) }}</span>
            </button>
            <button type="button" class="ot-tab" data-tab="cancelled">
                Cancelled <span class="ot-tab-count">{{ count($cancelledOrders) }}</span>
            </button>
        </div>

        <!-- ===== Status Summary Cards ===== -->
        <div class="ot-status-grid">
            <div class="ot-status-card ot-status-clickable" data-status="in_review">
                <div class="ot-status-icon">
                    <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="#2f5ede" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="margin:0 auto;"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><circle cx="11.5" cy="14.5" r="2.5"/></svg>
                </div>
                <div class="ot-status-label">In Review</div>
                <div class="ot-status-count">{{ $statusCounts['in_review'] }}</div>
            </div>
            <div class="ot-status-card ot-status-clickable" data-status="in_progress">
                <div class="ot-status-icon">
                    <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="#e0442e" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="margin:0 auto;"><circle cx="12" cy="12" r="3"/><path d="M19.4 15a1.65 1.65 0 0 0 .33 1.82l.06.06a2 2 0 1 1-2.83 2.83l-.06-.06a1.65 1.65 0 0 0-1.82-.33 1.65 1.65 0 0 0-1 1.51V21a2 2 0 0 1-4 0v-.09A1.65 1.65 0 0 0 9 19.4a1.65 1.65 0 0 0-1.82.33l-.06.06a2 2 0 1 1-2.83-2.83l.06-.06a1.65 1.65 0 0 0 .33-1.82 1.65 1.65 0 0 0-1.51-1H3a2 2 0 0 1 0-4h.09A1.65 1.65 0 0 0 4.6 9a1.65 1.65 0 0 0-.33-1.82l-.06-.06a2 2 0 1 1 2.83-2.83l.06.06a1.65 1.65 0 0 0 1.82.33H9a1.65 1.65 0 0 0 1-1.51V3a2 2 0 0 1 4 0v.09a1.65 1.65 0 0 0 1 1.51 1.65 1.65 0 0 0 1.82-.33l.06-.06a2 2 0 1 1 2.83 2.83l-.06.06a1.65 1.65 0 0 0-.33 1.82V9a1.65 1.65 0 0 0 1.51 1H21a2 2 0 0 1 0 4h-.09a1.65 1.65 0 0 0-1.51 1z"/></svg>
                </div>
                <div class="ot-status-label">In Progress</div>
                <div class="ot-status-count">{{ $statusCounts['in_progress'] }}</div>
            </div>
            <div class="ot-status-card ot-status-clickable" data-status="dispatched">
                <div class="ot-status-icon">
                    <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="#d97706" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="margin:0 auto;"><path d="M21 16V8a2 2 0 0 0-1-1.73l-7-4a2 2 0 0 0-2 0l-7 4A2 2 0 0 0 3 8v8a2 2 0 0 0 1 1.73l7 4a2 2 0 0 0 2 0l7-4A2 2 0 0 0 21 16z"/><polyline points="3.27 6.96 12 12.01 20.73 6.96"/><line x1="12" y1="22.08" x2="12" y2="12"/></svg>
                </div>
                <div class="ot-status-label">Ready to Dispatch</div>
                <div class="ot-status-count">{{ $statusCounts['ready_dispatch'] }}</div>
            </div>
            <div class="ot-status-card ot-status-clickable" data-status="dispatched">
                <div class="ot-status-icon">
                    <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="#2f5ede" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="margin:0 auto;"><rect x="1" y="3" width="15" height="13"/><polygon points="16 8 20 8 23 11 23 16 16 16 16 8"/><circle cx="5.5" cy="18.5" r="2.5"/><circle cx="18.5" cy="18.5" r="2.5"/></svg>
                </div>
                <div class="ot-status-label">Dispatched</div>
                <div class="ot-status-count">{{ $statusCounts['dispatched'] }}</div>
            </div>
            <div class="ot-status-card ot-status-clickable" data-status="delivered">
                <div class="ot-status-icon">
                    <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="#1d9e75" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="margin:0 auto;"><circle cx="12" cy="12" r="10"/><polyline points="9 12 11 14 15 10"/></svg>
                </div>
                <div class="ot-status-label">Delivered</div>
                <div class="ot-status-count">{{ $statusCounts['delivered'] }}</div>
            </div>
        </div>

        <!-- ===== Filters ===== -->
        <div class="ot-filters">
            <div class="ot-filter-field">
                <label>Outlet</label>
                <select id="filterOutlet">
                    <option value="">Select Outlet</option>
                    @foreach($outlets ?? [] as $o)
                        <option value="{{ $o->id }}" @if($o->id == ($filterOutlet ?? '')) selected @endif>{{ $o->outlet_name ?? $o->name }}</option>
                    @endforeach
                </select>
            </div>
            <div class="ot-filter-field">
                <label>Order ID</label>
                <input type="text" id="filterOrderId" placeholder="Enter Order ID" value="{{ $filterOrderId ?? '' }}">
            </div>
            <div class="ot-filter-field">
                <label>Date Range</label>
                <input type="date" id="filterDateRange" value="{{ $filterDate ?? '' }}">
            </div>
        </div>

        <div class="ot-filter-actions">
            <button type="button" class="ot-btn-apply" id="applyFiltersBtn">Apply Filters</button>
            @if(($filterOutlet ?? '') || ($filterOrderId ?? '') || ($filterDate ?? ''))
                <a href="{{ route('orders') }}" class="ot-btn-reset" style="text-decoration:none; display:inline-flex; align-items:center; justify-content:center;">Reset</a>
            @else
                <button type="button" class="ot-btn-reset" id="resetFiltersBtn">Reset</button>
            @endif
        </div>

        <!-- ===== Live Orders List ===== -->
        <div id="tabContent-live">
            <div class="ot-section-label">Live Orders ({{ count($liveOrders) }})</div>

            @foreach($liveOrders as $order)
                @php
                    $currentIndex = array_search($order['status'], $statusSteps);
                    $color = $statusColors[$order['status']];
                    $isDelivered = $order['status'] === 'delivered';
                    $fillPercent = $currentIndex > 0 ? ($currentIndex / (count($statusSteps) - 1)) * 100 : 0;
                @endphp

                <div class="ot-order-card live-order-card" data-status="{{ $order['status'] }}">
                    <div class="ot-order-top">
                        <div class="ot-order-icon" style="background: {{ $color['bg'] }}; color: {{ $color['fg'] }};">
                            @if($order['status'] === 'in_review')
                                <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><circle cx="11.5" cy="14.5" r="2.5"/></svg>
                            @elseif($order['status'] === 'in_progress')
                                <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="3"/><path d="M19.4 15a1.65 1.65 0 0 0 .33 1.82l.06.06a2 2 0 1 1-2.83 2.83l-.06-.06a1.65 1.65 0 0 0-1.82-.33 1.65 1.65 0 0 0-1 1.51V21a2 2 0 0 1-4 0v-.09A1.65 1.65 0 0 0 9 19.4a1.65 1.65 0 0 0-1.82.33l-.06.06a2 2 0 1 1-2.83-2.83l.06-.06a1.65 1.65 0 0 0 .33-1.82 1.65 1.65 0 0 0-1.51-1H3a2 2 0 0 1 0-4h.09A1.65 1.65 0 0 0 4.6 9a1.65 1.65 0 0 0-.33-1.82l-.06-.06a2 2 0 1 1 2.83-2.83l.06.06a1.65 1.65 0 0 0 1.82.33H9a1.65 1.65 0 0 0 1-1.51V3a2 2 0 0 1 4 0v.09a1.65 1.65 0 0 0 1 1.51 1.65 1.65 0 0 0 1.82-.33l.06-.06a2 2 0 1 1 2.83 2.83l-.06.06a1.65 1.65 0 0 0-.33 1.82V9a1.65 1.65 0 0 0 1.51 1H21a2 2 0 0 1 0 4h-.09a1.65 1.65 0 0 0-1.51 1z"/></svg>
                            @elseif($order['status'] === 'ready_dispatch')
                                <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 16V8a2 2 0 0 0-1-1.73l-7-4a2 2 0 0 0-2 0l-7 4A2 2 0 0 0 3 8v8a2 2 0 0 0 1 1.73l7 4a2 2 0 0 0 2 0l7-4A2 2 0 0 0 21 16z"/><polyline points="3.27 6.96 12 12.01 20.73 6.96"/><line x1="12" y1="22.08" x2="12" y2="12"/></svg>
                            @elseif($order['status'] === 'dispatched')
                                <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="1" y="3" width="15" height="13"/><polygon points="16 8 20 8 23 11 23 16 16 16 16 8"/><circle cx="5.5" cy="18.5" r="2.5"/><circle cx="18.5" cy="18.5" r="2.5"/></svg>
                            @else
                                <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><polyline points="9 12 11 14 15 10"/></svg>
                            @endif
                        </div>

                        <div class="ot-order-mid">
                            <div class="ot-order-id-row">
                                <span class="ot-order-id">{{ $order['id'] }}</span>
                                <span class="ot-status-badge" style="background: {{ $color['bg'] }}; color: {{ $color['fg'] }};">{{ $statusLabels[$order['status']] }}</span>
                                @if($order['can_cancel'] ?? false)
                                    <i class="fa-solid fa-circle-xmark" style="color:#e0442e; cursor:pointer; font-size:14px;" title="Cancel your order" onclick="cancelOrder('{{ $order['real_id'] }}')"></i>
                                @endif
                            </div>
                            <div class="ot-order-outlet">Outlet: {{ $order['outlet'] }}</div>
                            <div class="ot-order-meta">
                                <span>📋 Items: {{ $order['items'] }}</span>
                                <span>📅 {{ $order['date'] }}</span>
                                <a href="#" class="ot-view-order-toggle" data-toggle-id="{{ $order['real_id'] }}">👁 View Order</a>
                            </div>
                            @if($order['can_invoice'] ?? false)
                                <a href="{{ route('generateInvoiceAndDeliveryCharges.list', ['id' => $order['real_id']]) }}"
                                   onclick="window.open(this.href,'_blank','width=800,height=600'); return false;"
                                   style="font-size:11.5px; font-weight:700; color:#4f5fff; text-decoration:none; display:inline-block; margin-top:4px;">View Invoice →</a>
                            @endif

                            <div class="ot-order-items-table" id="orderDetails_{{ $order['real_id'] }}" style="display:none; margin-top:10px; border:1px solid #eef0f3; border-radius:10px; overflow:hidden;">
                                <table style="width:100%; border-collapse:collapse; font-size:12px;">
                                    <thead>
                                        <tr style="background:#f7f8fa;">
                                            <th style="text-align:left; padding:8px 10px; border-bottom:1px solid #eef0f3; color:#344054;">Product</th>
                                            <th style="text-align:left; padding:8px 10px; border-bottom:1px solid #eef0f3; color:#344054;">Qty</th>
                                            <th style="text-align:left; padding:8px 10px; border-bottom:1px solid #eef0f3; color:#344054;">Price</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        @forelse($order['order_items'] ?? [] as $item)
                                            <tr>
                                                <td style="padding:8px 10px; border-bottom:1px solid #f2f3f6;">{{ $item['product']['product_name'] ?? 'N/A' }}</td>
                                                <td style="padding:8px 10px; border-bottom:1px solid #f2f3f6;">{{ $item['quantity'] ?? '-' }}</td>
                                                <td style="padding:8px 10px; border-bottom:1px solid #f2f3f6;">₹{{ number_format($item['price'] ?? 0, 2) }}</td>
                                            </tr>
                                        @empty
                                            <tr><td colspan="3" style="padding:8px 10px;">No items found</td></tr>
                                        @endforelse
                                    </tbody>
                                </table>
                            </div>
                        </div>

                        <div class="ot-order-right">
                            @if($isDelivered)
                                <div class="ot-order-right-label">Delivered On</div>
                                <div class="ot-order-right-date" style="color:#1d9e75;">{{ $order['delivered_on'] }}</div>
                            @else
                                <div class="ot-order-right-label">Expected Delivery</div>
                                <div class="ot-order-right-date" style="color:#2f5ede;">{{ $order['expected_delivery'] }}</div>
                            @endif

                            <div class="ot-order-amount-label">Total Amount</div>
                            <div class="ot-order-amount">₹{{ number_format($order['total'], 2) }}</div>

                            <div class="ot-payment-badge" style="background: {{ $order['payment_status'] === 'Paid' ? '#e8f8f0' : '#fff3e0' }}; color: {{ $order['payment_status'] === 'Paid' ? '#1d9e75' : '#b5650a' }};">
                                {{ $order['payment_status'] }}
                            </div>

                            @if($order['can_pay'] ?? false)
                                <button type="button" class="ot-pay-now-btn checkout_pay" data-order-id="{{ $order['real_id'] }}" data-amount="{{ $order['remaining'] ?? $order['total'] }}">
                                    Pay Now ₹{{ number_format($order['remaining'] ?? $order['total'], 2) }}
                                </button>
                            @endif
                        </div>
                    </div>

                    <!-- ===== Progress Stepper ===== -->
                    <div class="ot-stepper">
                        <div class="ot-stepper-line-fill" style="width: calc({{ $fillPercent }}% - {{ $fillPercent > 0 ? '26px' : '0px' }});"></div>

                        @foreach($statusSteps as $index => $step)
                            @php
                                $stepState = $index < $currentIndex ? 'done' : ($index === $currentIndex ? ($isDelivered ? 'done' : 'current') : '');
                            @endphp
                            <div class="ot-step {{ $stepState }}">
                                <div class="ot-step-dot">
                                    @if($stepState === 'done')
                                        <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3"><polyline points="20 6 9 17 4 12"/></svg>
                                    @else
                                        {{ $index + 1 }}
                                    @endif
                                </div>
                                <div class="ot-step-label">{{ $statusLabels[$step] }}</div>
                            </div>
                        @endforeach
                    </div>
                </div>
            @endforeach
        </div>

        <div id="tabContent-history" style="display:none;">
            <div class="ot-section-label">Order History ({{ count($historyOrders) }})</div>

            @forelse($historyOrders as $order)
                <div class="ot-order-card">
                    <div class="ot-order-top">
                        <div class="ot-order-icon" style="background: #e8f8f0; color: #1d9e75;">
                            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><polyline points="9 12 11 14 15 10"/></svg>
                        </div>
                        <div class="ot-order-mid">
                            <div class="ot-order-id-row">
                                <span class="ot-order-id">{{ $order['id'] }}</span>
                                <span class="ot-status-badge" style="background:#e8f8f0; color:#1d9e75;">Delivered</span>
                            </div>
                            <div class="ot-order-outlet">Outlet: {{ $order['outlet'] }}</div>
                            <div class="ot-order-meta">
                                <span>📋 Items: {{ $order['items'] }}</span>
                                <span>📅 {{ $order['date'] }}</span>
                                <a href="#" class="ot-view-order-toggle" data-toggle-id="{{ $order['real_id'] }}">👁 View Order</a>
                            </div>
                            @if($order['can_invoice'] ?? false)
                                <a href="{{ route('generateInvoiceAndDeliveryCharges.list', ['id' => $order['real_id']]) }}"
                                   onclick="window.open(this.href,'_blank','width=800,height=600'); return false;"
                                   style="font-size:11.5px; font-weight:700; color:#4f5fff; text-decoration:none; display:inline-block; margin-top:4px;">View Invoice →</a>
                            @endif
                            <div class="ot-order-items-table" id="orderDetails_{{ $order['real_id'] }}" style="display:none; margin-top:10px; border:1px solid #eef0f3; border-radius:10px; overflow:hidden;">
                                <table style="width:100%; border-collapse:collapse; font-size:12px;">
                                    <thead><tr style="background:#f7f8fa;"><th style="text-align:left; padding:8px 10px;">Product</th><th style="text-align:left; padding:8px 10px;">Qty</th><th style="text-align:left; padding:8px 10px;">Price</th></tr></thead>
                                    <tbody>
                                        @forelse($order['order_items'] ?? [] as $item)
                                            <tr><td style="padding:8px 10px;">{{ $item['product']['product_name'] ?? 'N/A' }}</td><td style="padding:8px 10px;">{{ $item['quantity'] ?? '-' }}</td><td style="padding:8px 10px;">₹{{ number_format($item['price'] ?? 0, 2) }}</td></tr>
                                        @empty
                                            <tr><td colspan="3" style="padding:8px 10px;">No items found</td></tr>
                                        @endforelse
                                    </tbody>
                                </table>
                            </div>
                        </div>
                        <div class="ot-order-right">
                            <div class="ot-order-right-label">Delivered On</div>
                            <div class="ot-order-right-date" style="color:#1d9e75;">{{ $order['delivered_on'] }}</div>
                            <div class="ot-order-amount-label">Total Amount</div>
                            <div class="ot-order-amount">₹{{ number_format($order['total'], 2) }}</div>
                            <div class="ot-payment-badge" style="background:#e8f8f0; color:#1d9e75;">{{ $order['payment_status'] }}</div>
                        </div>
                    </div>
                </div>
            @empty
                <p style="color:#98a2b3; font-size:13px;">No past orders yet.</p>
            @endforelse
        </div>

        <div id="tabContent-cancelled" style="display:none;">
            <div class="ot-section-label">Cancelled Orders ({{ count($cancelledOrders) }})</div>

            @forelse($cancelledOrders as $order)
                <div class="ot-order-card">
                    <div class="ot-order-top">
                        <div class="ot-order-icon" style="background: #fdecea; color: #a4161a;">
                            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><line x1="15" y1="9" x2="9" y2="15"/><line x1="9" y1="9" x2="15" y2="15"/></svg>
                        </div>
                        <div class="ot-order-mid">
                            <div class="ot-order-id-row">
                                <span class="ot-order-id">{{ $order['id'] }}</span>
                                <span class="ot-status-badge" style="background:#fdecea; color:#a4161a;">Cancelled</span>
                            </div>
                            <div class="ot-order-outlet">Outlet: {{ $order['outlet'] }}</div>
                            <div class="ot-order-meta">
                                <span>📋 Items: {{ $order['items'] }}</span>
                                <span>📅 {{ $order['date'] }}</span>
                            </div>
                        </div>
                        <div class="ot-order-right">
                            <div class="ot-order-amount-label">Total Amount</div>
                            <div class="ot-order-amount">₹{{ number_format($order['total'], 2) }}</div>
                        </div>
                    </div>
                </div>
            @empty
                <p style="color:#98a2b3; font-size:13px;">No cancelled orders.</p>
            @endforelse
        </div>

    </div>
</div>

@endsection

@section('scripts')
<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
<script src="https://checkout.razorpay.com/v1/checkout.js"></script>
<script>
$(document).ready(function () {

    $('.ot-tab').on('click', function () {
        const tab = $(this).data('tab');

        $('.ot-tab').removeClass('active');
        $(this).addClass('active');

        $('#tabContent-live, #tabContent-history, #tabContent-cancelled').hide();
        $('#tabContent-' + tab).show();
    });

    // ===== Status summary cards — click to filter the Live Orders list =====
    $('.ot-status-clickable').on('click', function () {
        const $card = $(this);
        const status = $card.data('status');
        const wasActive = $card.hasClass('active');

        $('.ot-status-clickable').removeClass('active');

        if (wasActive) {
            // Clicking an already-active card clears the filter, shows everything
            $('.live-order-card').show();
            return;
        }

        $card.addClass('active');

        if (status === 'delivered') {
            // Delivered orders live in the History tab, not Live Orders —
            // switch tabs instead of filtering an empty Live list
            $('.ot-tab').removeClass('active');
            $('.ot-tab[data-tab="history"]').addClass('active');
            $('#tabContent-live, #tabContent-history, #tabContent-cancelled').hide();
            $('#tabContent-history').show();
            return;
        }

        // Make sure we're on the Live tab when filtering by a live status
        $('.ot-tab').removeClass('active');
        $('.ot-tab[data-tab="live"]').addClass('active');
        $('#tabContent-live, #tabContent-history, #tabContent-cancelled').hide();
        $('#tabContent-live').show();

        $('.live-order-card').each(function () {
            $(this).toggle($(this).data('status') === status);
        });
    });


    // ===== Real filters — navigate with query params, page re-fetches
    // filtered $liveOrders/$historyOrders/$cancelledOrders from the server =====
    $('#applyFiltersBtn').on('click', function () {
        const params = new URLSearchParams();

        const outletVal = $('#filterOutlet').val();
        const orderIdVal = $('#filterOrderId').val();
        const dateVal = $('#filterDateRange').val();

        if (outletVal) params.set('outlet_id', outletVal);
        if (orderIdVal) params.set('order_id_search', orderIdVal);
        if (dateVal) params.set('date_range', dateVal);

        window.location.href = window.location.pathname + (params.toString() ? '?' + params.toString() : '');
    });

    $('#resetFiltersBtn').on('click', function () {
        window.location.href = window.location.pathname;
    });

    // ===== View Order — toggle item table =====
    $(document).on('click', '.ot-view-order-toggle', function (e) {
        e.preventDefault();
        const id = $(this).data('toggle-id');
        $('#orderDetails_' + id).slideToggle();
    });

    // ===== Cancel order =====
    window.cancelOrder = function (orderId) {
        Swal.fire({
            title: 'Are you sure?',
            text: "You won't be able to revert this!",
            icon: 'warning',
            showCancelButton: true,
            confirmButtonColor: '#4f5fff',
            cancelButtonColor: '#e0442e',
            confirmButtonText: 'Yes, cancel it!'
        }).then((result) => {
            if (result.isConfirmed) {
                $.ajax({
                    url: '/cancel-order/' + orderId,
                    type: 'POST',
                    data: { '_token': '{{ csrf_token() }}' },
                    success: function () {
                        Swal.fire('Cancelled!', 'Your order has been cancelled.', 'success')
                            .then(() => { window.location.reload(); });
                    },
                    error: function () {
                        Swal.fire('Error!', 'Something went wrong.', 'error');
                    }
                });
            }
        });
    };

    // ===== Pay Now — Razorpay flow, same endpoint as desktop =====
    $(document).on('click', '.checkout_pay', function () {
        const orderId = $(this).data('order-id');
        const totalDiscountValue = $(this).data('amount');

        fetch('/updatepay-order', {
            method: 'POST',
            body: JSON.stringify({ order_id: orderId, payment_status: 'paid', totalDiscountValue: totalDiscountValue }),
            headers: { 'Content-Type': 'application/json', 'X-CSRF-TOKEN': '{{ csrf_token() }}' }
        })
        .then(res => res.json())
        .then(responseData => {
            if (!responseData.order_id || !responseData.amount) {
                Swal.fire({ title: 'Error', text: 'Invalid payment details. Please try again.', icon: 'error' });
                return;
            }
            const options = {
                key: responseData.razorpay_key,
                amount: responseData.amount,
                currency: 'INR',
                name: 'Zonik',
                description: 'Order Payment',
                order_id: responseData.order_id,
                callback_url: '/handle-payment-update',
                theme: { color: '#4f5fff' }
            };
            if (typeof Razorpay !== 'undefined') {
                new Razorpay(options).open();
            } else {
                Swal.fire({ title: 'Error', text: 'Payment gateway failed to load.', icon: 'error' });
            }
        })
        .catch(() => {
            Swal.fire({ title: 'Error', text: 'An error occurred. Please try again.', icon: 'error' });
        });
    });

});
</script>
@endsection
